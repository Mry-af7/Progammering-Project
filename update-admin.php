<?php require __DIR__."/vendor/autoload.php"; $app = require_once __DIR__."/bootstrap/app.php"; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); use App\Models\User; $admin = User::where("email", "admin@erasmus.be")->first(); if ($admin) { $admin->update(["password" => password_hash("admin123", PASSWORD_BCRYPT)]); echo "Admin password updated successfully!\n"; } else { echo "Admin user not found.\n"; }
